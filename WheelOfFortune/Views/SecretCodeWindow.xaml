<Window x:Class="FortuneWheel.Views.SecretCodeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Секретный код"
        Height="180" Width="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<TextBlock Grid.Row="0"
                   Text="🔐 Введите секретный код:"
                   FontSize="14" FontWeight="SemiBold"
                   Margin="0,0,0,10"/>

		<TextBox Grid.Row="1"
                 x:Name="CodeInput"
                 FontSize="16" Padding="8,6"
                 Margin="0,0,0,15"
                 CharacterCasing="Upper"/>

		<TextBlock Grid.Row="2"
                   x:Name="HintText"
                   Text="Подсказка: попробуйте «ПОМОЩЬ»"
                   FontSize="11" FontStyle="Italic"
                   Foreground="#888"
                   Margin="0,0,0,10"/>

		<TextBlock Grid.Row="3"
                   x:Name="ErrorText"
                   Foreground="#C62828"
                   FontSize="12"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"/>

		<StackPanel Grid.Row="4" Orientation="Horizontal"
                    HorizontalAlignment="Right">
			<Button Content="OK" Width="80" Padding="6,4"
                    Margin="0,0,8,0" IsDefault="True"
                    Click="OnOkClick"/>
			<Button Content="Отмена" Width="80" Padding="6,4"
                    IsCancel="True"/>
		</StackPanel>
	</Grid>
</Window>